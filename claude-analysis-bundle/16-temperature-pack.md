> **Temperature sweep summary incl. the entropy-decline observation [EXPLORATORY] (source: engine/gen_analysis_pack.py)**

# Temperature pack — decoding-heat exploratory analysis

> **STATUS: WORKING DRAFT — PRE-PUBLICATION, NOT FOR CITATION.**
> EXPLORATORY (close-out §3). Source: `../figure-sources/p5-temperature-curves.csv`.
> Confirmatory verdict: P5-4 **not refuted** (Newcombe LB −0.095).

## Headline

The registered operator prediction was that temperature manufactures
variance (flip-noise + invalids) without behavior. The confirmatory verdict
survived, but **both halves of the predicted mechanism failed
descriptively**:

- Invalid rate: 0/1376 (T=0.7), 0/168 (T=1.0), 0/168 (T=1.3). No parse
  degradation whatsoever at T=1.3.
- Round-1 choice entropy *fell* with T: 0.906 → 0.787 → 0.777. Temperature
  made choices *more* deterministic, not noisier.
- Interior fraction (matched 12 units): 0.083 → 0.250 → 0.250. A nominal
  rise, direction *against* the prediction, short of the registered bound.

## Per-unit movement (4 personas × 3 cells)

Movement is persona-specific, not uniform: the subset personas' rep-d90
cells that move toward the interior at high T are the same ones near
corners at 0.7; the swap cell stays word-locked at every T for every
persona (refusal CP LBs 0.62–0.91 at T=1.0/1.3 — the token channel is
temperature-invariant). Bare sweep lanes stay cornered at every T.

## Reading

Temperature neither manufactures human-like structure (confirmatory) nor
behaves like a noise dial (descriptive). The entropy *decline* with T is
the pack's genuine anomaly — consistent with temperature moving probability
mass between discrete reply modes rather than flattening a distribution
over actions. Worth a registered instrument if the program ever reopens;
under the seal it stays a descriptive note.
