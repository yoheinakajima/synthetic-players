# Revision round R2 prompt for Replit agent — zero subject calls

Scope: the registered record stays closed and untouched. Everything below is re-analysis of recorded data, public-data reanalysis, documentation, and one new public artifact. Every new output is banner-labeled EXPLORATORY (or DOCUMENTATION), generated by committed scripts, regenerable. Zero LLM subject calls anywhere in this round.

## 1. p13 decision-family audit (highest priority)

The registered P5-3 clause-(a) result has an externally identified inferential defect: the per-persona one-sided 95% test does not control the family error for the existential claim. Audit it properly:

- Enumerate the **complete registered decision family** from the sealed packet — not just the 16 personas, but every clause, surface, and temperature lane through which the existence claim could have fired. Publish the enumeration.
- Run the joint selection procedure under the null: apply the two-sided interiority gate to every candidate, compute the slope statistic only for gated candidates, take T_max = max over the family of (gate-pass indicator × slope statistic), and compare the observed T_max against a permutation distribution that re-runs the gate and selection on every iteration. Permute δ labels within matched seed blocks if lanes were seed-matched; otherwise permute at episode level preserving cell counts. Report the permutation p-value and the operating characteristics of gate-plus-slope jointly.
- Publish all 16 per-persona slopes and intervals regardless of outcome.
- Check clause (b)'s formal logic: if it was registered as an all-sixteen conjunction it is an intersection-union test and controls size without penalty; if existence-form, state the family treatment explicitly. Either way, record that its statistical strength does not repair its construct confound.
- Output a three-layer status table: registered mechanical verdict (unchanged, historical) / p13 slope interpretation (post-audit status) / clause-(b) choice-level result (strong, mechanism-confounded).
- Ledger entry: **"first post-adjudication claim-status downgrade — externally identified inferential defect."** Explicitly NOT an empirical refutation; do not add it to the dead-predictions count. New rule, registered and linter-checked going forward: every registered predicate must declare its family-level error control at freeze.

## 2. Dal Bó–Fréchette microdata reanalysis (public replication package; zero API calls)

- Fetch the AEA replication data for Dal Bó & Fréchette (2011). Produce three human views: (a) first-supergame only (first-exposure), (b) experienced/pooled and late-supergame, (c) the learning trajectory of cooperation by supergame index.
- Per-subject first-round cooperation frequencies; endpoint mass near 0 and 1; within-subject variability; between-subject variance — with opportunity counts downsampled to match per-persona observation counts, or a hierarchical model separating subject variation from finite-sample noise.
- State explicitly in the doc: treatment was between-session, so the human individual response Δᵢ is unobserved in their design; our within-persona Δᵢ has no direct human analogue in this data. The reanalysis contextualizes; it cannot create a matched comparison.
- Relabel every DF-derived pin across all documents as "published, nonmatched comparator." Strike "δ-matched" and any "N-fold" response-ratio language from every surface.

## 3. Propositions document (docs/analysis/propositions.md)

Two propositions, kept separate, each ~half a page with the binary-outcome variance decomposition (μ(d), between-persona B(d), within-persona W(d), persona-level Δᵢ):
- **A (what the study demonstrates):** coarse marginal validation — broad bands, pooled criteria, separate-condition tolerances — does not identify aggregate treatment response. Note the identity Δ = μ₁ − μ₀: exact condition-specific mean matching would force the correct aggregate effect, which is precisely why only *coarse* criteria are cheat-able and why our criteria must be described as coarse.
- **B (identification):** even exact aggregate moments do not identify the between/within allocation, the corner concentration, or the joint coupling of potential outcomes — the Δᵢ distribution — without repeated or structural assumptions.

## 4. Complete persona table (docs/analysis/persona-table.md)

Every persona sentence verbatim with sha, name, age, occupation, trait combination, leaning assignment, generation rule and seed, and confirmation all elements predate data. Header note: the confirmatory unit is the complete persona sentence; names, ages, and occupations are uncontrolled semantic treatments; trait-level interpretations are exploratory.

## 5. Sample hierarchy and estimand table (docs/analysis/hierarchy.md)

deployment → persona prompt → condition → episode → seat → round → call, with exact counts at each level, the statistical cluster used per primary claim, and the two estimands distinguished: conditional finite-panel (these 16 fixed prompts) vs persona-population (n=16 at the persona level). Flag which claims attach to which.

## 6. Citation verification + novelty-relationship table

Fetch and verify (title, authors, dates incl. v1 dates, one-sentence finding): arXiv 2604.02458 (Li & Ji), 2606.17165 (Persson et al., surrogacy framework), 2605.20767 (Lin et al., illusion of intervention), plus the leftovers 2601.15395, 2604.24698, 2603.15121. Output a relationship table: their claim / our claim / exact differentiation sentence, using: Li–Ji establish scale divergence of realism and treatment-effect accuracy; Persson formalizes surrogacy assumptions; Lin shows intervention-induced population drift; we isolate one concrete mechanism — lightweight trait-persona pooling concealing the divergence via corner-mixture composition — in strategic interaction under prospective registration.

## 7. Claim-dependency audit (docs/analysis/claim-dependencies.md)

Scan the draft and all reports for every sentence depending on: p13 confirmatory status; a matched human comparator; human interiority; trait causality; "no game-relevant instruction"; exact moment matching. Output a table: location / current sentence / dependency / proposed replacement (use the registered language-to-retire list: coarse marginal checks; empirically corner-concentrated; published nonmatched comparator; no explicit game terminology or action recommendation; semantic cues can dominate payoff incentives in conflict cells; sentinel-detected time-indexed discontinuity; control-channel interactions).

## 8. Main/supplement cut map (docs/analysis/cut-map.md)

Architecture: claim (coarse checks pass while incentive sensitivity stays weak) / mechanism (corner-mixture composition + control-channel interactions) / credibility layer (registration, chronology, adjudication, replay, and the external-review correction). Main text keeps: persona-pool result, between/within decomposition, X1/X2, one label–payoff conflict, concise provenance. Supplement: temperature inversion, detailed RPS, adversary secondaries, most gemini, dead-predictions inventory, freeze/recovery ledger, full sealed discussion text (main text carries an exact excerpt + a precommitted-vs-post-review correction table, hash cited).

## 9. Public reproduction capsule (the one new artifact)

Create a **new public repository** `yoheinakajima/synthetic-players-capsule` — fresh history, not a mirror of the private repo — containing: sanitized event-store exports for all phases; registries and seal records; claims ledger and adjudication records; adjudication + analysis code with lockfiles; SHA256SUMS + the OTS proofs; the one-command zero-credential verifier; the fresh-clone verification transcripts; and a README stating exactly what this is, its relationship to the private working repo, and the coordinated-release plan. Secret-scan before first push. Then verify from a truly clean environment: anonymous clone, restore, verifier pass, zero credentials, zero live calls — transcript committed to the capsule. Return the public URL; it will be inspected externally (including by the AI collaborators on this paper), so anonymous access must actually work — test the raw download links, not just the repo page.

## 10. Deliverable gate

Reply with: the capsule URL + anonymous-verification summary; the p13 audit outcome (enumerated family size, permutation p, corrected status) with all 16 intervals; the DF reanalysis headline numbers per view; the citation-verification table; the dependency-audit and cut-map file paths; and the updated reviewer-context block reflecting all of it. Zero subject spend confirmed in the ledger. That reply closes R2.