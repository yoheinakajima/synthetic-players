# Post-finals prompt for Replit agent — analysis handoff + drop prep

The registered record is closed and stays closed. Everything below is re-analysis of recorded data, packaging, and process hardening — zero new subject calls, scope-seal fully in force, every new output banner-labeled EXPLORATORY, generated by committed scripts, regenerable from the event store. Purpose: prepare the bundle for the pre-final paper analysis (happening off-repo), answer the specific questions the Branch-2 verdicts opened, and make drop day a one-command affair.

## 1. Targeted post-verdict analyses (EXPLORATORY, docs/analysis/post-verdict/)

1. **Clause-(b) confound anatomy.** The 15/16 swap passes conflate "refuses the dominated option" with "follows the COOPERATE word" — in the swap cell those are the same choice. Produce: per-persona × per-leaning swap-choice table (all 16, rates + CIs); an explicit note stating what the recorded data can and cannot decide here — the balanced-payoff × persona cell that would de-confound it was never run and is scope-sealed to future work; and the cross-reference to the Phase 4 bare balanced-payoff probe (0/40 both models). No spin, just the anatomy.
2. **p13 deep-dive.** Its full persona card verbatim (traits, preamble, sha); every p13 cell across the design with rates and gate status (the corrected count: 4 interior cells); round-level trajectories for its two interior δ cells; p13 vs the other 15 on every recorded metric; and a trait-gradient table — personas sharing 2 of 3 traits with p13 and how each behaved on the same cells. Label the gradient analysis clearly exploratory.
3. **Interior-cell census.** The corrected full list: every interior persona-cell in Phase 5 (persona, cell, T, rate, CI) — six personas' worth. The branch hinges on existence; the census is the exhibit.
4. **P5-2 decomposition.** The pooled 0.128 task-consistent share, broken out: rep S2-conflict cells (the payoff-opposed, unconfounded subset) separately from the swap conflict cell, each with CIs, per leaning. The rep subset is the clean persona-dominance claim; quantify it on its own.
5. **Entropy-vs-T anomaly.** Show how entropy was computed, then decompose the falling-with-T result by lane (bare vs persona) and cell family, and test whether aggregation (Simpson-type) explains it. If it survives decomposition, say so plainly; if it's an artifact, show the artifact.

## 2. The Claude analysis bundle (the handoff)

Build `claude-analysis-bundle/` — a curated, read-ordered package sized for an LLM analysis session, delivered as a release asset AND a single downloadable archive I can grab and upload elsewhere. Contents, in INDEX.md read order:

1. Phase 5 final report; Phase 4 final report; Phase 3 report (as sealed).
2. Branch-selection record + the sealed Branch 2 text + the P5-2 variant retained.
3. claims-ledger.md (the .csv too); dead-predictions-final.md; human-anchor-scorecard.md; corner-map.md.
4. The five post-verdict analyses from §1.
5. persona-pack key tables; temperature-pack summary; distribution-pack summary; stability-compendium.
6. outcome-blind decisions ledger (all four completions with rationales); instance-ledger; ops-meta.
7. program-synthesis-DRAFT.

Token discipline: prefer the .md summaries; include compact CSVs only where the table IS the content (claims ledger, corner map, interior census, per-persona swap table). Target the whole bundle under ~2 MB of text. Every file self-describing (one-line header: what this is, confirmatory or exploratory, source script).

## 3. Process hardening (code, not notes)

- Mechanize "seal last, review first": the close-seal step refuses to run unless an architect-review artifact exists and is newer than every artifact being sealed. The post-seal defect fix must be the last of its kind.
- Land the two linter extensions from the outcome-blind rulings as actual checks now: conflict-cell coding must be pinned at freeze wherever word and role dissociate; branch-selection axes must enumerate their bearing predicates explicitly. Acceptance tests reproduce this phase's two gaps as freeze failures.

## 4. Drop runbook

`docs/drop-runbook.md` — the coordinated-drop day as a checklist a tired human can execute: flip visibility → anonymous-clone and anonymous asset-download verification → `ots upgrade` on every stamp and verify → flip the publicationStatus switch → confirm banners cleared only where intended → post-drop smoke (README renders, releases resolve, fresh-clone verification rerun from a truly anonymous environment). Include the rollback line for each step. No step executes now.

## 5. Push + gate

Commit and push everything; repo stays private; no visibility change under any circumstances. Reply with: the bundle's release-asset URL and archive size, the INDEX listing, confirmation the §1 analyses and §3 gates are committed with their acceptance tests green, and the runbook path. That reply ends this program's agent work until drop day.
