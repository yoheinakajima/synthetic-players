name: preprint-v14-final (historical manual rebuild)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical versioned workflow retained for manual provenance checks only. Use Final arXiv release for the canonical paper.'
