name: Final arXiv release (maintenance hold)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Release workflow temporarily held while canonical source replaces the damaged payload.'
