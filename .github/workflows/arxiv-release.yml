name: Final arXiv release (staging paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Staging final release payload; automatic PR build temporarily paused.'
