name: Historical review preprint rebuild (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The versioned review build is archived. Use arxiv-release.yml for the canonical paper.'
