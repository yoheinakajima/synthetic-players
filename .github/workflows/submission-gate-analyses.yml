name: Historical submission-gate rebuild (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The historical reviewer-build workflow is archived. Use arxiv-release.yml for the canonical paper and analyses.'
